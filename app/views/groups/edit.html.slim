h1.page-header
  'Edit Group ##{@group.id}
= render 'form'
= link_to 'delete', @group, method: :delete, data: { confirm: 'Delete this group for ever?' },
  class: 'btn btn-danger pull-right', role: 'button'
